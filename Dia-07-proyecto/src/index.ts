/**
 * Día 07 — Proyecto Final
 * =========================
 * Aplicación de línea de comandos que consume JSONPlaceholder.
 *
 * Funcionalidades:
 * 1. Listar usuarios — GET /users
 * 2. Ver posts de un usuario — GET /posts?userId=X
 * 3. Top de usuarios por posts — GET /posts y /users para contar
 *
 * Implementa un menú interactivo con readline o prompt-sync.
 * Usa los tipos de types.ts, las funciones de api.ts, y los colores del enum.
 *
 * Requisitos:
 * - Tipado estricto con interfaces
 * - Genéricos (fetchJSON<T>)
 * - Async/await con fetch
 * - Manejo de errores con try/catch + unknown
 * - Clases (ApiError)
 * - Enums (Color)
 * - Módulos (import/export)
 */

// ─── Imports ────────────────────────────────────────────────────────────────
// import type { Usuario, Post, PostConUsuario, AccionMenu } from "./types.js";
// import { Color, colorizar } from "./types.js";
// import { obtenerUsuarios, obtenerPosts } from "./api.js";

// ─── Funciones auxiliares ───────────────────────────────────────────────────
// function mostrarMenu(): void { }
// function pausar(): Promise<void> { }

// ─── Funcionalidades ────────────────────────────────────────────────────────
// async function listarUsuarios(): Promise<void> { }
// async function verPostsDeUsuario(): Promise<void> { }
// async function topUsuariosPorPosts(): Promise<void> { }

// ─── Función principal ──────────────────────────────────────────────────────
// async function main(): Promise<void> { }
